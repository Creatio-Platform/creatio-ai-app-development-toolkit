---
name: analytics-widgets
description: creatio freedom ui analytics & widget placement guidance — how to add, arrange, size, and style metric (indicator) widgets and charts so they look native and balanced. apply proactively whenever creating or editing a dashboard, home page, list-page analytics view, or a form-page widget area, or whenever the user mentions: dashboard, widget, metric, indicator, kpi, tile, chart, bar chart, column chart, pie/doughnut/donut, line/spline trend, gauge, pivot table, list widget, summaries panel, "by category/status/channel", analytics, "agent 360", "command center". consult it BEFORE choosing a widget type, deciding how many widgets per row, setting a widget's size/width, choosing its background style, or grouping widgets into sections — so the result keeps the native "plain white" card look, the correct default size per widget type, and the canonical metric-band-then-chart-grid layout. keywords: creatio, freedom ui, dashboard, widget, metric, indicator, chart, donut, kpi tile, analytics view, summaries, widget style, widget size, plain.
---

INTERIM (review by 2026-12-18): analytics-widgets is the clio-owned single source of truth for analytics-widget authoring; the ADAC toolkit ships only a thin pointer. Migration tracking: ENG-TBD. Do not duplicate this content elsewhere.

# Routing skill — authoritative guidance lives in clio MCP

This skill carries no analytics-widget authoring content of its own. It is a thin pointer into the clio MCP guidance catalog, which is the single source of truth for widget placement, layout, sizing, styling, and payload.

Call get-guidance name=analytics-widgets (index), which routes to get-guidance name=dashboards (layout/sizing/styling), get-guidance name=indicator-widget (payload), and the placement-contexts reference for non-dashboard surfaces. Do NOT author from this skill alone.
